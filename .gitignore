# ── GHDL / simulation artefacts ──────────────────────────────────────────
work/
work_basys/
*.cf
*.ghw
*.o
*.vcd
*.log
# generated dumps: encoder_out*, py_*, pixels_*, reference_*, scales, ...
*.txt
dfx_runtime.txt

# ── Vivado artefacts (keep the .tcl build scripts, drop everything else) ──
vivado_synth_test/*
!vivado_synth_test/*.tcl
*.bit
*.mcs
*.prm
*.dcp
*.rpt
*.jou
*.backup.log
.Xil/

# ── Python ───────────────────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.pyc

# ── MNIST data (re-downloaded automatically by torchvision) ──────────────
data/

# ── Local / private (chat session exports, scratch, editor state) ────────
.claude/
scratch/
extracted_sessions/

# ── OS ───────────────────────────────────────────────────────────────────
Thumbs.db
.DS_Store

# ── Unreferenced AI-generated diagram (kept locally, not in the public repo) ─
vit_fpga_architecture.png

# ── cifar10_vit: generated simulation vectors and training checkpoints ──────
# Vectors are regenerated by `make vectors` in cifar10_vit/sim; the trained
# int8 .bin weights ARE tracked, but the intermediate .npz checkpoint is not.
cifar10_vit/sim/vectors/
cifar10_vit/weights_int8/*.npz
